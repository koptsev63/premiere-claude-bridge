node_modules/
*.log
.DS_Store
mcp-server/node_modules/
__pycache__/
*.pyc
# python virtualenv for the analysis tools (opencv, etc.) — never commit
.venv/
venv/
/tmp/

# generated NLE project files (FcpxmlAdapter writes to cwd) — never commit
/*.fcpxml
/*.otio
# local credentials must never enter this public repo
*.env
.env
